<Window x:Class="MedicineManager.MainAppWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MedicineManager"
        mc:Ignorable="d"
        Title="MainAppWindow" Height="450" Width="800">

    <Window.Resources>
        <local:DateColorConverter x:Key="DateColorConverter"/>
    </Window.Resources>

    <Grid>
        <Calendar HorizontalAlignment="Center" 
                  VerticalAlignment="Center"
                  SelectionMode="SingleDate"
                  SelectedDatesChanged="Calendar_SelectedDatesChanged">

            <Calendar.CalendarDayButtonStyle>
                <Style TargetType="CalendarDayButton">
                    <Setter Property="Background" 
                            Value="{Binding Date, RelativeSource={RelativeSource Self}, Converter={StaticResource DateColorConverter}}" />

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="CalendarDayButton">
                                <Grid>
                                    <Rectangle Fill="{TemplateBinding Background}" Opacity="0.6"/>
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"
                                                      TextElement.Foreground="Black"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Calendar.CalendarDayButtonStyle>
        </Calendar>
    </Grid>
</Window>