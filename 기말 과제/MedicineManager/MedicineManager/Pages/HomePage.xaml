<Page x:Class="MedicineManager.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="450"
      Title="HomePage" Background="#F5F5F7">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Viewbox Grid.Row="1" VerticalAlignment="Top" Stretch="Uniform">
                    <Calendar x:Name="ExpiryCalendar" SelectionMode="SingleDate"/>
                </Viewbox>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="15" Margin="0,0,5,0">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.2"/>
                </Border.Effect>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="약 종류" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <Ellipse Width="80" Height="80" Stroke="#4CAF50" StrokeThickness="8" Margin="5"/>
                    <TextBlock Text="원형 차트" Foreground="Gray" HorizontalAlignment="Center" FontSize="10"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="15" Margin="5,0,0,0">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.2"/>
                </Border.Effect>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="폐기 예정" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <Grid Height="80" Width="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Rectangle x:Name="BarMonth1" Grid.Column="0" Width="20" Height="30" Fill="#EF9A9A" VerticalAlignment="Bottom"/>
                        <Rectangle x:Name="BarMonth2" Grid.Column="1" Width="20" Height="50" Fill="#EF5350" VerticalAlignment="Bottom"/>
                        <Rectangle x:Name="BarMonth3" Grid.Column="2" Width="20" Height="20" Fill="#C62828" VerticalAlignment="Bottom"/>
                    </Grid>
                    <TextBlock Text="이번달 / 다음달 / 다다음달" Foreground="Gray" HorizontalAlignment="Center" FontSize="9" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>